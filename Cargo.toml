[workspace]
resolver = "2"
members = [
    "codegen",   # comline-codegen-rust — the Rust `code` / `lib` generator
]
# Room for `libgen/` (FFI / dylib — G2c), `runtime/`, `std-extra/` as they land.
# See ComlineProject/docs → Design → Runtime & generation repository structure.

[profile.release]
opt-level = 2
